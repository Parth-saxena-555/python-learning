# slicesing of name(string)in python
name = "parthsaxena"
slicename = name[0:4]
print(slicename)
# sliceing meaing cutting string into parts by index format its , 
# so we use 0 index to start and end upto 4th index,
#  we can do it like 2sd index to start and 5th index to end
# eg:
print(slicename[2:5]) # so its output is rt because r is on 2sd index and ton 5th index
